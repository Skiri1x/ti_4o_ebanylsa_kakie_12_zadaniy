for N in range(1,1000):
    a=bin(N)[2:]
    b=a.count('1')
    if int(b)%2==0:
        b=str(b)+'0'
    else:
        b=str(b)+'1'
    if int(b)%2==0:
        b=str(b)+'0'
    else:
        b=str(b)+'1'
    b=str(b)
    R=int(b,2)
    if R>93:
        print(R)
#в пизду это твоё 5 задание, это анрил, я уже битых 20 минут сижу ошибку за ошибкой решаю, короче дальше пойдут 14, 5 повторим потом, я не ебу как их делать, даже с файлами из гитхаба
